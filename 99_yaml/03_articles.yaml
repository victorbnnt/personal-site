apiVersion: apps/v1
kind: Deployment
metadata:
  name: articles
spec:
  replicas: 1
  selector:
    matchLabels:
      app: articles
  template:
    metadata:
      labels:
        app: articles
    spec:
      containers:
      - name: articles
        image: victorbnnt/personal:articles
        imagePullPolicy: Always
        ports:
        - containerPort: 80
        env:
        - name: TITLE
          value: "Articles"
---
apiVersion: v1
kind: Service
metadata:
  name: articles
spec:
  type: ClusterIP
  ports:
  - port: 80
  selector:
    app: articles
